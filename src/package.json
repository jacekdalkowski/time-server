{
  "name": "time-server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile-dev": "babel . --out-dir .compiled --ignore 'node_modules/*' --source-maps --watch && cp -R 'node_modules/' '.compiled/node_modules/'",
    "compile-prod": "babel . --out-dir .compiled --ignore 'node_modules/*' && cp -R 'node_modules/' '.compiled/node_modules/'"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "moment": "^2.22.2",
    "restify": "^7.2.2",
    "restify-errors": "^6.1.1"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1"
  },
  "babel": {
    "presets": [
      "es2015"
    ]
  }
}
